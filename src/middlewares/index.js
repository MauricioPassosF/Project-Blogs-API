const loginValidations = require('./loginValidations');
const tokenValidations = require('./tokenValidations');
const categoriesValidations = require('./categoriesValidations');
const postValidations = require('./postValidations');

module.exports = {
  loginValidations,
  tokenValidations,
  categoriesValidations,
  postValidations,
};